name: flutter_news
description: 使用flutter技术打造的新闻类APP
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0
  event_bus: ^1.1.0
  cupertino_icons: ^0.1.2
  flutter_webview_plugin: 0.3.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
   - assets/config/channel.json
